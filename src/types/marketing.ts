/**
 * TypeScript interfaces for marketing feature
 * Re-exports all types from mock data files for easy importing
 */

// Re-export types from mock files
export type { AdCampaign } from "~mocks/ad-platforms";

export type { HubSpotContact, HubSpotDeal } from "~mocks/hubspot-data";
